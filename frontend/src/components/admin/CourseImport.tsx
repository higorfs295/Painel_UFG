// ADMIN: importar nova matriz (JSON no formato do seed) — caminho para os 2 cursos futuros
// TODO: implementar conforme ESPECIFICACAO.md (§7 contrato de API, §10 componentes).
export default function CourseImport() {
  return <div data-todo="CourseImport" />;
}
