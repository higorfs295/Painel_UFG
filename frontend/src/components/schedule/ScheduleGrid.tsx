// grade Seg–Dom x slots SIGAA (+linhas extras), blocos de aula, conflitos, pintura via pointer events
// TODO: implementar conforme ESPECIFICACAO.md (§7 contrato de API, §10 componentes).
export default function ScheduleGrid() {
  return <div data-todo="ScheduleGrid" />;
}
